export interface HomePage {
  title: string
  subtitle: string
  buttonText: string
}

export interface Service {
  _id: string
  title: string
  description: string
}

export interface Portfolio {
  _id: string
  title: string
  client: string
}